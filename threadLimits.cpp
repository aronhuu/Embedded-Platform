#include "mbed.h"
/*
EventFlags event;


Thread threadLimits(osPriorityNormal, 512); // 1K stack size

void limits_thread();

void limits_thread()
{
	uint32_t flags = 0x0;
	
	while (true) {
		event.wait_all(0x1);
		flags = event.get();
	}
}*/